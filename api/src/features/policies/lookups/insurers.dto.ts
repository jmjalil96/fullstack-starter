/**
 * DTO for available insurers endpoint
 */

/**
 * Available insurers response (for picklist/dropdown)
 */
export interface AvailableInsurerResponse {
  id: string
  name: string
  code: string | null
}
